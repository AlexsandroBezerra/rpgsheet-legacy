import styled from 'styled-components'
import { Box, Image } from 'rebass/styled-components'

export const ProfileDataContainer = styled(Box)`
  margin-right: 1rem;
  text-align: right;
`

export const AvatarImage = styled(Image)`
  width: 3rem;
  height: 3rem;
  border-radius: 100%;
`
